[Unit]
Description=ATS FastAPI Service
After=network.target

[Service]
User=ubuntu
WorkingDirectory=/opt/backend
ExecStart=/opt/backend/.venv/bin/uvicorn app.main:app --host 0.0.0.0 --port 8000
Restart=always

[Install]
WantedBy=multi-user.target
